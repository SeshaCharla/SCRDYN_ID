import numpy as np
from scipy.signal import  decimate

q = 5

def decimate_time_TD(t, tskips):
    """ Get one sample every q samples"""
    n = len(t)
    t_dec = np.zeros(np.floor(n/q))

def decimate_TD(x, tskips):
    """Decimate with the skips"""
    x = len
    x = np.zeros()